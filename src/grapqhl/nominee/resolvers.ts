import { Nominee } from '../../db'
import { Context } from '../../interfaces'

export default {
  // Query: {
  //   async nominees(parent: {}, args: {}, context: Context): Promise<Nominee[]> {
  //     const nominees = await Nominee.findAll()
  //     if (!nominees) {
  //       throw new Error('There are no nominees')
  //     }
  //     return nominees
  //   },
  // },
  // Mutation: {}
}
